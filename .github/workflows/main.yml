on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - develop
      - master
    types: [closed]
name: 🚀 Deploy VPS
jobs:
  racknerd-deploy:
    name: 🎉 Deploy to Racknerd VPS
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: 107.174.156.33
        username: ${{ secrets.FTP_NHUT_RACKNERD_VPS_USER }}
        password: ${{ secrets.FTP_NHUT_RACKNERD_VPS_PASS }}
